apply plugin: "war"

bootWar {
    mainClassName = "pt.up.hs.project.ProjectApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
